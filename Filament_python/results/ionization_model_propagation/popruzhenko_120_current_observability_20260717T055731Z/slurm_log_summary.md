# Slurm log summary

- Job `175050` (`pop120obs`) completed with exit code `0:0` after 9,696.60 s.
- The propagation reported `z = 1.300/1.300 m` (100.00%), then saved the NPZ
  and passed its 15,000-record diagnostic check.
- The final logged pulse energy was about `2.050e-03 J` (`-5.61%` relative),
  and the final plasma diagnostics retained distinct raw/applied values in the
  output schema (the recorded values were equal for this run).
- The Slurm stderr file is empty.  No fatal or numerical-error line was found
  in the captured stdout tail.

The full stdout and stderr are retained remotely only.  Their SHA256 values
are recorded in `archive_manifest.json`.
